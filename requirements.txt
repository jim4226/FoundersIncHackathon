fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
numpy>=1.26
scipy>=1.13
python-osc>=1.9
anthropic>=0.40

# The gesture and desk bridges are WebSocket clients; without this they fail at
# connect time and surface only as "disconnected (ModuleNotFoundError)".
websockets>=13.0

# Desk camera object detection (desk/desk_server.py).
opencv-python>=4.9

# Optional: hand landmarks on the desk camera. Without it the desk still detects
# objects, it just cannot report which one your hand is on. Also needs the model:
#   curl -o desk/hand_landmarker.task https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task
# On headless Linux: apt-get install libgles2 libegl1
mediapipe>=0.10.21

# Only needed for EEG_SOURCE=brainflow. Native BLE; dongles deprecated in 5.22.0.
brainflow>=5.22.2
